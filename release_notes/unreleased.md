**Unreleased**

- Encoded ERS resource identifiers as single path segments to prevent access outside the selected resource endpoint.
- Enabled TLS certificate verification by default for Cisco ISE connections.
